query RefreshWorkItemsQuery(
  $namespaceFullPath: ID!,
  $ids: [WorkItemID!] = [],
) {
  namespace(fullPath: $namespaceFullPath) {
    ...simpleNamespace
    workItems(ids: $ids, includeDescendants: true) {
      nodes {
        ...bareWorkItem
      }
    }
  }
}
